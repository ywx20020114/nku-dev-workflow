---
name: nku-dev-workflow
description: 按角色化流程驱动多服务全栈项目从需求到上线。读取全部服务知识库理解全貌，7 个子 Agent 顺序执行，针对目标服务开发。
---

# nku-dev-workflow — 研发流程编排

面向多服务全栈项目的 AI 驱动研发流程。主 Agent 加载全部服务知识库理解全局上下文，按顺序启动 7 个子 Agent，针对**目标服务**执行开发。

## 核心设计

```
                     ┌─ 读取全部知识库（理解全貌）─┐
                     │                            │
  knowledge_base/    │  开发只针对一个目标服务       │
  ├── user-service/  │  ← 可能是目标 ──────────────│
  ├── order-service/ │  ← 提供上下文（知道有哪些接口）│
  ├── gateway/       │  ← 提供上下文               │
  └── admin-frontend/│  ← 提供上下文               │
                     └────────────────────────────┘
```

## 触发条件

| 用户说法 | 效果 |
|---------|------|
| "帮我在 <service> 中实现 xxx" | 明确目标服务，直接开始 |
| "帮我实现 xxx 功能" | 主 Agent 从知识库推断目标服务，不明确时询问 |
| "自动跑完" / "继续" | 跳过后续确认连续执行 |

## 配置文件

执行前先读取 `config.json`：
- `services` — 所有服务列表，每个包含：name、type、local_path、构建/测试/运行命令
- `knowledge_base` — 知识库根目录，其下每个子目录对应一个服务的知识文档
- `agent_mode.confirm_each_step` — 是否每步确认

## 前置：加载全部知识库

```
knowledge_base/
├── <service-a>/    ← 学习时产出
│   ├── _overview.md
│   ├── server.md
│   └── modules/
├── <service-b>/
│   └── ...
└── <frontend-x>/
    └── ...
```

1. 遍历 `knowledge_base/` 下所有子目录，加载全部服务的知识文档
2. 构建全局视图：所有服务的接口清单、数据库表、服务间调用关系
3. 若知识库为空（无子目录或无内容），提示先执行 nku-dev-knowledge

## 目标服务定位

收到需求后，主 Agent 从全部知识库中定位目标服务：

| 情况 | 处理 |
|------|------|
| 用户明确指定了 service | 直接使用 |
| 需求涉及某个服务的 Entity/表 | 该服务即为目标 |
| 需求涉及某接口路径 | 查找哪个服务暴露了该路径 |
| 无法从上下文判断 | 列出候选服务，询问用户 |
| 需求涉及多个后端服务 | 提示用户拆分需求，一次只改一个服务 |

## 流程编排

```
用户输入需求
    ↓
主 Agent 加载全部知识库 → 定位目标服务 → 启动 ① Analyst
    → 输出改动清单（标注目标服务 + 受影响的其他服务） → ⏸️ 确认
    ↓
主 Agent 启动 ② Coder
    → 在目标服务创建 feature 分支 → 按分层实现 → commit
    ↓
主 Agent 启动 ③ Compiler
    → 编译目标服务（其他服务不动）
    ↓
主 Agent 启动 ④ Tester
    → 测试目标服务 + 联调验证（如涉及与其他服务的交互）
    ↓
主 Agent 启动 ⑤ Reviewer
    → 评审目标服务代码，关注服务间调用是否合理
    ↓
主 Agent 启动 ⑥ Deployer
    → 部署目标服务 → 健康检查 → ⏸️ 确认
    ↓
主 Agent 启动 ⑦ Learner
    → 增量更新目标服务的知识库 → git push
```

## 执行规则

- 每个子 Agent 执行完毕后默认暂停，等待确认
- 子 Agent 失败时暂停，不继续
- 产出写入 `runs/<task-id>/<role>.md`
- 编译/测试/部署只针对目标服务，其他服务不动

## 断点续跑

检查 `runs/<task-id>/` 目录下的角色产出文件，从断点处继续。

## 子 Agent 传入内容

每个子 Agent 收到：
1. 之前步骤的产出文档
2. **全部服务的知识文档**（理解全局上下文）
3. **目标服务信息**（name、local_path、构建命令等）
